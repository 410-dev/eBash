#!/bin/bash
@import Foundation/string
@import Foundation/out

function Files.list() {
	if [[ $(isNull "$1") ]]; then
		println "Syntax error in: Files.list - Parameter not properly given."
		exit
	else
		ls "-1$2" "$1"
	fi
}

export -f Files.list