#!/bin/bash
function Application.setContext() {
	if [[ $(isNull "$1") ]]; then
		println "Syntax error in: Application.setContext"
	else
		export APPLICATION_CONTEXT="$1"
	fi
}

export -f Application.setContext