#!/bin/bash
function @prerun() {
	if [[ ! -z "$1" ]]; then
		if [[ -f "${EBHOME}/lib/$1" ]]; then
			source "$(dirname "$0")/lib/$1"
		else
			echo "Error at @import: Library \"$1\" not found"
			exit
		fi
	else
		echo "Error at @import: Missing target"
		exit
	fi
}

export -f @prerun